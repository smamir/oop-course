#include<iostream>
#include "INTEGER.h"
#include "POINT.h"

using namespace std;

int main()
{
    /*INTEGER a(10), b(20);
    INTEGER c,e, d(100);

    c=(a/b) - (a+b) + (b*a);
    c.display();

    ++c;
    c.display();

    c++;
    c.display();

    --c;
    c.display();
    c--;
    c.display();

    if(c>=d)
    {
        cout<< "c is greater or equal to d" << endl;
    }
    else
        cout << "not greater or equal" << endl;*/

    Point p1(0,0) , p2(2,3);

    p1.display();


    return 0;
}
